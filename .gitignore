# deps
node_modules/

# secrets — NEVER commit
.env
.env.*
*.key
*.pem
.perso/
credentials

# work artifacts
*.log
tmp/
out/
*.tmp.*

# resume state files (contain local absolute paths · projectSeq) / dev scratch
*.dubresume.json
dubbing-resume.json
.dubbing-done.json
_*.mjs

# 내부 작업 문서 — docs/ 는 공개 GitHub Pages 루트이므로 절대 커밋 금지
docs/superpowers/
.superpowers/
